<%= simple_form_for (@personnage) do |f| %>
  <%= f.input :name %>
  <%= f.input :category, :collection => %w[good bad] %>
  <%= f.input :role, :collection => %w[sherif thief marshall cowboy criminal doctor] %>
  <%= f.input :price %>
  <%= f.input :photo, as: :file %>
  <%= f.submit %>
<% end %>
